import React from "react"

import Layout from "components/Layout"

const NotFoundPage = () => (
  <Layout>
    <h1>NOT FOUND</h1>
    <p>You just hit a route that does not exist... that sucks!</p>
  </Layout>
)

export default NotFoundPage
